# Dream archive — read and written only by /dream

Every rule that ever existed is either active in SKILL.md, subliminal here, or an epitaph here. Nothing vanishes without a trace.

## Subliminal

<!-- Full rule bodies (priority 0–2), same format as SKILL.md, retaining complete evidence history,
     plus a line: demoted: <date>, <reason>
     The working agent never sees these. /dream revives one to SKILL.md at p3 when a fresh
     steering event matches its pattern — never on mere passage of time. -->

## Forgotten

<!-- Epitaphs only (priority −1). Format:
     R<n> — <one sentence rule> — forgotten <date>: <reason>
     Re-introducing a forgotten rule requires citing its epitaph and justifying why the
     situation changed. -->

## Trigger probes

<!-- Maintained in lockstep with active rules. Format:
     load: R<n> — <concrete task-time moment the description must catch>
     skip: <concrete task-time moment the description must NOT catch>
     One load probe per active rule, minimum. Skip probes are standing — they accumulate
     from observed or near-miss false triggers and are not tied to any rule.
     /dream Step 6 desk-checks the regenerated description against every probe.
     A failing probe indicts the description, not the rule. -->

load: R1 — about to write or restructure a SKILL.md, its frontmatter, or its bundled references
skip: implementing an ordinary code change with no skill files involved
skip: answering a question about what a skill does, without editing it
